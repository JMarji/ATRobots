#include <iostream>
#include <string>
#include "../atrlib.hpp"
using namespace std;

int main(void)
{
  int high, low, result;
  result = customArray(high,low);
  result = customArray(low, high);
  return 0;
}
